# 根据 test_customer_login.yaml 更新 CustomerLoginPage

## 更新时间
2026-07-03

## YAML 文件位置
`E:\ui_auto\tests\test_customer_login.yaml`

## 更新内容

### 1. 元素定位器（第35-97行）
根据 YAML 实际流程更新了所有元素定位器：

| 元素 | YAML 行号 | 更新后的选择器 |
|------|-----------|----------------|
| 密码登录切换 | 第19行 | `text=密码登录` |
| 用户名输入框 | 第24,29行 | `input[placeholder*='用户名']`, `input[type='tel']`, `input[type='text']` |
| 密码输入框 | 第34行 | `input[type='password']`, `input[placeholder*='密码']` |
| 登录按钮 | 第39行 | `text=立即登录` |
| 首次登录弹窗 | 第44行 | `text=我知道了` |
| 公司列表弹框 | 第49行 | `text=公司列表` |
| 公司选择 | 第54行 | `text=上海燃气崇明有限公司` |
| 登录成功标识 | 第59行 | `text=您好！上海燃气崇明有限公司` |

### 2. customer_login() 方法（第110-173行）
根据 YAML 步骤更新了登录流程：

| YAML 步骤 | 描述 | 更新内容 |
|-----------|------|----------|
| 第17-20行 | 切换密码登录 | timeout 从 3000 改为 5000 |
| 第22-25行 | 等待账号输入框加载 | 添加了 `wait_for_any_visible` 步骤 |
| 第27-30行 | 输入手机号 | 保持使用 `fill_any` |
| 第32-35行 | 输入密码 | 保持使用 `fill_any` |
| 第37-40行 | 点击立即登录 | timeout 从默认改为 5000 |
| 第42-45行 | 关闭首次弹窗 | timeout 从 3000 改为 10000 |
| 第47-50行 | 等待公司列表弹框 | 使用 `self.COMPANY_LIST_DIALOG` |
| 第52-55行 | 选择公司 | timeout 从 5000 改为 10000 |
| 第57-61行 | 断言登录成功 | timeout 从 8000 改为 15000 |

### 3. 其他改进
- 添加了详细的步骤日志（步骤1-9）
- 从 `.env` 自动读取 `company_name`（如果未提供）
- 断言超时时间根据实际流程调整

## 测试验证

### 运行测试
```bash
cd f:\UI_AUTO
python -m pytest test_quick_login.py -v -s
```

### 预期结果
1. ✅ 打开登录页面
2. ✅ 切换密码登录
3. ✅ 输入用户名：18501375833
4. ✅ 输入密码：123qwe
5. ✅ 点击立即登录
6. ✅ 关闭首次弹窗（如果有）
7. ✅ 选择公司：上海燃气崇明有限公司（如果有）
8. ✅ 断言登录成功：看到"您好！上海燃气崇明有限公司"

## 后续工作
1. 运行测试验证登录流程
2. 如果失败，检查截图和日志
3. 根据实际页面调整选择器
